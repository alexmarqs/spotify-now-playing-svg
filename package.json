{
  "name": "spotify-now-playing-svg-gen",
  "private": true,
  "version": "1.0.0",
  "description": "Spotify Now Playing Serverless API SVG Generator",
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel --prod",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "preact": "^10.19.0",
    "preact-render-to-string": "^5.2.0"
  }
}
